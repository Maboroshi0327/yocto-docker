# yocto-docker
🐋 Dockerized Build Environment for Yocto Project A ready-to-use, reproducible Docker image pre-configured with all necessary dependencies for building Yocto/OpenEmbedded distributions. Features Ubuntu-based layers, essential build tools, and non-root user setup.

## Pre-built Image

You can pull the pre-built image from Docker Hub:

```bash
docker pull maboroshi327/yocto:latest
```
